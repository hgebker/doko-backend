spring:
  profiles:
    active: @spring.profiles.active@

springdoc:
  api-docs:
    path: "/api-docs"
  swagger-ui:
    path: "/api-ui"

server:
  port: 8080

amazon:
  dynamodb:
    endpoint: http://localhost:8000